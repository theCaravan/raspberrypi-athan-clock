[tool.poetry]
name = "raspberrypi-athan-clock"
version = "0.1.0"
description = ""
authors = ["King Hadi I via Jahsh <hadi.alaoua@gmail.com>"]

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
poetry-core = ">=1.0.0"
requests = "^2.32.5"
gpiozero = "^2.0.1"
unicornhatmini = { version = "*", markers = "sys_platform == 'linux'" }

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
